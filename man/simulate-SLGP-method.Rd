% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{simulate,SLGP-method}
\alias{simulate,SLGP-method}
\title{Simulate responses from a fitted SLGP model}
\usage{
\S4method{simulate}{SLGP}(
  object,
  nsim = 1,
  seed = NULL,
  newdata,
  interpolateBasisFun = "WNN",
  nIntegral = 101,
  nDiscret = 101,
  discrete = FALSE,
  ...
)
}
\arguments{
\item{object}{An object of class \code{\link{SLGP-class}}.}

\item{nsim}{Number of samples to draw at each covariate value. Default 1.}

\item{seed}{Optional integer seed for reproducibility.}

\item{newdata}{A \code{data.frame} of covariate values.}

\item{interpolateBasisFun}{Integral-approximation scheme; default \code{"WNN"}.}

\item{nIntegral, nDiscret}{Integration / discretisation resolutions.}

\item{discrete}{Logical; treat the response as discrete. Default \code{FALSE}.}

\item{...}{Ignored.}
}
\value{
A \code{data.frame} of sampled responses with the covariate columns
from \code{newdata}.
}
\description{
This method provides the standard user interface for simulating from a fitted
\code{\link{SLGP-class}} object.
}
\examples{
set.seed(1)
d <- data.frame(
  x = rep(seq(0, 1, length.out = 6), each = 5)
)
d$y <- rnorm(nrow(d), mean = sin(2 * pi * d$x), sd = 0.2)

fit <- slgp(
  y ~ x,
  data = d,
  method = "MAP",
  basisFunctionsUsed = "RFF",
  predictorsLower = 0,
  predictorsUpper = 1,
  responseRange = range(d$y),
  opts_BasisFun = list(nFreq = 20, MatParam = 5 / 2),
  seed = 1
)

## Draw 10 samples from the conditional distributions at two locations
sim <- simulate(
  fit,
  newdata = data.frame(x = c(0.25, 0.75)),
  nsim = 10
)

head(sim)

}
