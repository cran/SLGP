% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{update,SLGP-method}
\alias{update,SLGP-method}
\title{Re-fit an SLGP model under a new method or with new data}
\usage{
\S4method{update}{SLGP}(
  object,
  method,
  newdata = NULL,
  epsilonStart = NULL,
  interpolateBasisFun = "WNN",
  nIntegral = 101,
  nDiscret = 101,
  discrete = FALSE,
  hyperparams = NULL,
  sigmaEstimationMethod = "none",
  seed = NULL,
  opts = list(),
  trend = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{An object of class \code{\link{SLGP-class}}.}

\item{method}{Estimation method: one of \code{"MCMC"}, \code{"MAP"},
\code{"Laplace"}.}

\item{newdata}{Optional new \code{data.frame}; if \code{NULL}, the model's
stored data are reused.}

\item{epsilonStart}{Optional initial coefficient values.}

\item{interpolateBasisFun}{Integral-approximation scheme; default \code{"WNN"}.}

\item{nIntegral, nDiscret}{Integration / discretisation resolutions.}

\item{discrete}{Logical; whether the response is treated as discrete.
Default \code{FALSE}.}

\item{hyperparams}{Optional list of hyperparameters; if \code{NULL}, those of
\code{object} are reused.}

\item{sigmaEstimationMethod}{Variance-selection method; default \code{"none"}.}

\item{seed}{Optional integer seed.}

\item{opts}{List of method-specific options (e.g. \code{stan_chains},
\code{stan_iter} for MCMC, \code{ndraws} for Laplace).}

\item{trend}{Optional trend function.}

\item{verbose}{Logical; verbosity. Default \code{FALSE}.}

\item{...}{Ignored.}
}
\value{
An updated object of class \code{\link{SLGP-class}}.
}
\description{
Standard-generic front end to \code{\link{retrainSLGP}}: re-estimates an
existing \code{\link{SLGP-class}} model, optionally with new data, under a
chosen estimation method, reusing the existing basis and ranges.
}
\examples{
set.seed(1)
d <- data.frame(
  x = rep(seq(0, 1, length.out = 6), each = 5)
)
d$y <- rnorm(nrow(d), mean = sin(2 * pi * d$x), sd = 0.2)

prior <- slgp(
  y ~ x,
  data = d,
  method = "none",
  basisFunctionsUsed = "RFF",
  predictorsLower = 0,
  predictorsUpper = 1,
  responseRange = range(d$y),
  opts_BasisFun = list(nFreq = 20, MatParam = 5 / 2),
  seed = 1
)

## Refit the same model structure by MAP
fit_map <- update(prior, method = "MAP")

}
\seealso{
\code{\link{retrainSLGP}} for the low-level routine.
}
